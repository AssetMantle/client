akka {
    actor {
        provider = cluster
    }
    remote.artery {
        canonical {
          hostname = "localhost"
          port = 9000
        }
      }
    cluster {
        shutdown-after-unsuccessful-join-seed-nodes = 60s
        seed-nodes = [
          "akka://client@localhost:9000",
          ]
    }
    coordinated-shutdown.exit-jvm = on
}


